[project]
name = "genai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "akshay", email = "akshaykumar143143ak@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.14",
]

[project.scripts]
genai = "genai:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
